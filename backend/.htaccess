# Set environment variable for Database Password
SetEnv DB_PASSWORD "R#t5FMwQ1"

# Security: Block access to sensitive files
<Files ".htaccess">
  Require all denied
</Files>

<Files "*.log">
  Require all denied
</Files>

<Files "*.config">
  Require all denied
</Files>

# Add any other directives needed for your API or server setup below
<IfModule mod_rewrite.c>
  RewriteEngine On
  # Block direct access to sensitive file types
  RewriteRule \.(log|config|htaccess)$ - [F,L]
</IfModule> 